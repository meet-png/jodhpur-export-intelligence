# JEIS — SLIM runtime deps for the deployed Streamlit dashboard ONLY.
#
# Streamlit Community Cloud auto-installs this root requirements.txt. The
# dashboard (streamlit_app.py) reads pre-computed CSVs only — including a
# pre-fit guar forecast — so it needs just these four pure/wheel libraries.
# No Prophet/Stan build, no statsmodels/scipy, no Java/tabula, no DB driver:
# the deploy is fast and has zero compile steps.
#
# Lower bounds (not exact pins) on purpose: this is a disposable demo
# environment where Streamlit Cloud may run any Python (3.10–3.13). Letting
# pip resolve a wheel-available compatible set makes the deploy bulletproof
# regardless of the runner's Python. Exact, reproducible pins live where
# they matter — the pipeline/CI: requirements-pipeline.txt.
#
# To run the FULL ETL pipeline / notebooks / CI instead:
#   pip install -r requirements-pipeline.txt

streamlit>=1.38
pandas>=2.2
numpy>=1.26
plotly>=5.22
