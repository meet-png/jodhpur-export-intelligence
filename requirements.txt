# JEIS — SLIM runtime deps for the deployed Streamlit dashboard ONLY.
#
# Streamlit Community Cloud auto-installs this root requirements.txt. The
# dashboard (streamlit_app.py) reads pre-computed CSVs and only needs these
# five packages — keeping this list minimal makes the deploy fast and
# bulletproof (no Prophet/Stan build, no Java/tabula, no DB drivers).
#
# To run the FULL ETL pipeline / notebooks / CI, install the complete
# pinned environment instead:
#   pip install -r requirements-pipeline.txt
#
# Pinned to the same versions as the pipeline env; tested on Python 3.11.

streamlit==1.38.0
pandas==2.2.2
numpy==1.26.4
plotly==5.22.0
statsmodels==0.14.2      # SARIMAX for the live guar-forecast tab
